Day1

heading tag goes to <h6>


format document with 
    install prettier extenson
    right click>> format document with>> configur default format>> prettier
    go to setting>> setting>> search on save >> check format on save


Day2

while using _blank in <a link this create the new tap in the server without exiting our webpage
for example:
    <a href="https://www.youtube.com/hashtag/youtubelink">youtube</a>   #this exist our website
    <a href="https://www.youtube.com/hashtag/youtubelink" target="_blank"> youtube</a>     #this doesnot exist our website


block element 
     always starts from new line 
     consume whole horizontal space 
     for ex. p, h, div.. 


inline element
    starts from available space 
    consume space as much as it required
    for ex. span , a , botton ,

<br> tag for break of line 
<hr> tag creates a bold line horizontal
<botton>  </botton> for creating botton like send 

format document with 
    install prettier extenson
    right click>> format document with>> configur default format>> prettier
    go to setting>> setting>> search on save >> check format on save

prep tag makes white space 


for making form we use <form> tag and used <div> tag to make  a single block 


format document with 
    install prettier extenson
    right click>> format document with>> configur default format>> prettier
    go to setting>> setting>> search on save >> check format on save

type = 'a', 'i','1'

html code is run by brower 
simatric is the proper way that  brower can understand 
boiler plate  ! 
extenson .html
atribute inside opening tag 
inline tag 
block tag
lorem for random tag 
list    order list , unorder list
for table 
     use  inside  table tag

inline css
internal css in 
external css

block 
takes whole horizontal space
can have width and height 

inline 
takes size as much as it required
cannot have weight and height
does not support margin top and button 

inline-block 
takes size as much as it required
can have width and height
    
                                        21st december
    file and folder
. include same folder
.. include the parents folder 
like example ../../

.means its own folder
../ means its parents folder
/is used to say arko 
.../ is not possible 
